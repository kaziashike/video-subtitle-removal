import cv2; import numpy as np; from paddleocr import PaddleOCR; cap = cv2.VideoCapture("tests/test.mp4"); ret, frame = cap.read(); ocr = PaddleOCR(use_angle_cls=True, lang="en"); result = ocr.ocr(frame); print("dt_polys:", result[0].dt_polys); print("Type of dt_polys:", type(result[0].dt_polys)); print("Length of dt_polys:", len(result[0].dt_polys)); print("First polygon:", result[0].dt_polys[0]); print("Type of first polygon:", type(result[0].dt_polys[0])); cap.release()
