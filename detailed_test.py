import cv2; import numpy as np; from paddleocr import PaddleOCR; cap = cv2.VideoCapture("tests/test.mp4"); ret, frame = cap.read(); print("Frame shape:", frame.shape); ocr = PaddleOCR(use_angle_cls=True, lang="en"); result = ocr.ocr(frame); print("Type of result:", type(result)); print("Length of result:", len(result)); print("Type of result[0]:", type(result[0])); print("Content of result[0]:", result[0]); cap.release()
